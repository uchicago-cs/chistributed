To run the simple example, do

$ python broker.py -e "python examples/node.py" -s simple.chi

This sets the main node executable to running "python examples/node.py" and then runs the script contained in "simple.chi"

